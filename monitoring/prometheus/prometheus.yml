global:
  scrape_interval: 10s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]

  # API FastAPI du graphe multi-agent HorRAGor — expose /metrics (Phase 11)
  - job_name: horragor-api
    static_configs:
      - targets: ["api:8000"]
